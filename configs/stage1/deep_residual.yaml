_base_: base.yaml
experiment_name: stage1_deep_residual
stage1:
  ranker:
    name: deep_residual
    params: {d_model: 48, use_transformer: true, n_transformer_layers: 1, use_gnn: true, n_gnn_layers: 2, use_residual: true,
             alpha_init: 0.1, dropout: 0.2, node_dropout: 0.1, feature_noise: 0.05, lr: 0.001, weight_decay: 0.001,
             epochs: 30, patience: 6, candidates_per_query: 20, ensemble_k: 3, base_ranker: extra_trees}
